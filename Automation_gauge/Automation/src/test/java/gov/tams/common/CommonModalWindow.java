package gov.tams.common;

import org.openqa.selenium.WebDriver;

//wip
public class CommonModalWindow extends CommonPage {
    protected CommonModalWindow(WebDriver webDriver) {
        super(webDriver);
    }
}
